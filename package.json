{
  "name": "braceframe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "icons": "node scripts/make-icons.mjs",
    "guides": "node scripts/export-guides.mjs",
    "check:captura": "node scripts/check-captura.mjs",
    "check:header": "node scripts/check-header.mjs",
    "dev": "vite",
    "build": "npm run icons && npm run guides && npm run check:captura && npm run check:header && tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "sharp": "^0.34.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-pwa": "^1.0.1"
  }
}
